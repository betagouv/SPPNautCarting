{
    "scripts": {
        "build": "rm -rf compiled_static && parcel build",
        "watch": "rm -rf compiled_static && parcel watch",
        "lint": "prettier --check ."
    },
    "source": [
        "frontend/carting.js"
    ],
    "targets": {
        "default": {
            "distDir": "./compiled_static"
        }
    },
    "prettier": {
        "trailingComma": "all",
        "printWidth": 88,
        "tabWidth": 4,
        "semi": false,
        "overrides": [
            {
                "files": "*yml",
                "options": {
                    "tabWidth": 2
                }
            }
        ]
    },
    "dependencies": {
        "@hotwired/stimulus": "^3.2.1",
        "ol": "^7.2.2",
        "parcel": "^2.8.3",
        "prettier": "^2.8.6"
    }
}
